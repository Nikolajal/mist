@PACKAGE_INIT@

# mistConfig.cmake — generated by CMake, do not edit.
#
# Allows downstream projects to do:
#
#   find_package(mist REQUIRED)
#   target_link_libraries(my_target PRIVATE mist::mist)
#
# The @PACKAGE_INIT@ macro (expanded by configure_package_config_file)
# provides set_and_check() and check_required_components() helpers.

include("${CMAKE_CURRENT_LIST_DIR}/mistTargets.cmake")

check_required_components(mist)
